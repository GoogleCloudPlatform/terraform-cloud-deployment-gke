apiVersion: blueprints.cloud.google.com/v1alpha1
kind: BlueprintMetadata
metadata:
  name: terraform-cloud-deployment-gke-helm-display
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  info:
    title: helm
    source:
      repo: https://github.com/GoogleCloudPlatform/terraform-cloud-deployment-gke.git
      sourceType: git
      dir: /infra/modules/helm
  ui:
    input:
      variables:
        chart_folder_name:
          name: chart_folder_name
          title: Chart Folder Name
        entries:
          name: entries
          title: Entries
        secret_entries:
          name: secret_entries
          title: Secret Entries
