apiVersion: blueprints.cloud.google.com/v1alpha1
kind: BlueprintMetadata
metadata:
  name: terraform-cloud-deployment-gke-networking-display
  annotations:
    config.kubernetes.io/local-config: "true"
spec:
  info:
    title: networking
    source:
      repo: https://github.com/GoogleCloudPlatform/terraform-cloud-deployment-gke.git
      sourceType: git
      dir: /infra/modules/networking
  ui:
    input:
      variables:
        health_check_allow_ports:
          name: health_check_allow_ports
          title: Health Check Allow Ports
        project_id:
          name: project_id
          title: Project Id
